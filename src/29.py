def greet_user(name):
    """
    Function to greet user with their name

    Args:
        name (str): The name of the user

    Returns:
        str: A message indicating that the user is greeted
    """
    return f"Hello, {name}!"

print(greet_user("Alice"))
